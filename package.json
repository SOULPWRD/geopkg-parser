{
    "name": "gpkg-parser",
    "type": "module",
    "version": "0.1",
    "author": {
        "name": "Martin Pravda"
    },
    "scripts": {
        "build": "esbuild ./sqlite --bundle --format=esm --outfile=dist/geopkg.js",
        "build:min": "esbuild ./sqlite --bundle --format=esm --minify --outfile=dist/geopkg.min.js"
    },
    "license": "MIT",
    "devDependencies": {
        "acorn": "latest",
        "acorn-walk": "latest",
        "esbuild": "0.24.2"
    }
}
